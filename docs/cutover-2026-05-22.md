# Cut-over 작업 기록 — 2026-05-22

## 목표

기존 3개 운영 디렉토리(`~/security-log-monitor`, `~/siem-ai-detector`, `~/siem-ai-analyst`)와 그 위의 도커 스택을, 모노레포 `~/SIEM-Trinity` 로 단일화한다. **데이터(Loki/Grafana/Prometheus/Wazuh 볼륨) 무손실 유지**가 핵심 제약.

## 사전 상태 (실측, 2026-05-22 14:46 KST)

### 호스트
- `kangminlog` (운영 본체), uptime 26d
- Backup: `restic` 매일 02:00 (`/etc/cron.d/daily-backup`), 마지막 성공 = 오늘 02:31 → **현재 시점 도커 볼륨 전체가 restic 스냅샷에 보관됨** (롤백 안전망)

### SIEM 관련 컨테이너 (compose project 라벨 기준)
| Container | Project (디렉토리) |
|---|---|
| `loki`, `promtail`, `wazuh-manager`, `prometheus`, `node-exporter`, `grafana` | `security-log-monitor` (`~/security-log-monitor/`) |
| `siem-api` | `siem-ai-detector` (`~/siem-ai-detector/`) |
| (없음 — 도커화 안 됨) | `~/siem-ai-analyst` |

### 보존 대상 볼륨
- `security-log-monitor_loki-data` (Loki 청크)
- `security-log-monitor_grafana-data` (대시보드·유저 DB)
- `security-log-monitor_prometheus-data` (시계열, 30d retention)
- `security-log-monitor_wazuh-data` (OSSEC 룰·로그)

### 보존 대상 네트워크
- `security-log-monitor_default` — `detection-api` 가 외부 참조 (`LOKI_NETWORK_NAME`)

## 마이그레이션 전략

[.env.example:30](../.env.example#L30) 에 이미 명시된 "운영 서버: 기존 폴더명 유지" 경로를 채택.

### 패치 ① — compose project name 변경
- 파일: [01-collection/docker-compose.yml:1](../01-collection/docker-compose.yml#L1)
- 변경: `name: siem-trinity` → `name: security-log-monitor`
- 효과: 새 compose 가 만드는 볼륨 prefix 가 `security-log-monitor_*` 로 떨어져 **기존 볼륨을 그대로 재사용**

### 패치 ② — 루트 `.env` 생성
- 파일: [`.env`](../.env) (gitignore 대상)
- 내용:
  - `HOST_BIND_IP=100.x.x.x` (Tailscale IP, 기존 노출과 동일)
  - `LOKI_NETWORK_NAME=security-log-monitor_default` (`02-detection` → 기존 네트워크 참조)
  - `GF_ADMIN_USER`, `GF_ADMIN_PASSWORD` (기존 운영값 유지 — 값은 저장소에 기록하지 않는다)

## 실행 순서

1. 패치 ①·② 적용 ✓
2. `docker compose config` 로 렌더 결과 검증
3. 기존 스택 정지: `cd ~/security-log-monitor && docker compose down` / `cd ~/siem-ai-detector && docker compose down`
4. 새 스택 기동: `cd ~/SIEM-Trinity && ./start.sh` (또는 단계별)
5. 검증: `docker ps`, Loki·Grafana·Prometheus 헬스체크, 기존 대시보드 보존 확인
6. 작업 결과 본 문서에 기록

## 롤백 경로

문제 시:
```bash
cd ~/SIEM-Trinity && docker compose -f 01-collection/docker-compose.yml down
cd ~/security-log-monitor && docker compose up -d
cd ~/siem-ai-detector && docker compose up -d
```
볼륨이 동일하므로 데이터 그대로 복귀. 최악의 경우 `restic restore` 로 오늘 02:30 스냅샷 복원 가능.

---

## 실행 로그

### 14:52 — 패치 적용
- [01-collection/docker-compose.yml](../01-collection/docker-compose.yml) `siem-trinity` → `security-log-monitor` 일괄 치환 (`name:` L1 + `networks.default.name:` L333)
- [.env](../.env) 생성: `HOST_BIND_IP=100.x.x.x`, `LOKI_NETWORK_NAME=security-log-monitor_default`, Grafana 자격증명
- `docker compose config` dry-run → 모든 volume 이 `security-log-monitor_*` 로 정확히 렌더됨 ✓

### 14:54 — 기존 스택 정지
```
cd ~/security-log-monitor && docker compose down  # 6개 컨테이너 정지·삭제, 볼륨 보존
cd ~/siem-ai-detector   && docker compose down  # siem-api 정지
```
✓ 네트워크 `security-log-monitor_default` 도 깔끔히 분리 (잔여 attach 없음 확인)

### 14:55 — 01-collection 기동
```
cd ~/SIEM-Trinity/01-collection && docker compose up -d
```
✓ loki, promtail, wazuh-manager, prometheus, node-exporter, grafana — 6개 모두 Started
✓ 기존 볼륨(`security-log-monitor_*loki-data/grafana-data/prometheus-data/wazuh-data`) 그대로 mount

### 14:58 — 03-intelligence 기동 (포트 충돌 → 패치)
- 1차 시도: `Bind for 127.0.0.1:11434 failed: port is already allocated` (dodgers-ollama-1 점유)
- **패치 결정 (B)**: [03-intelligence/docker-compose.yml:7](../03-intelligence/docker-compose.yml#L7) 의 호스트 포트만 11434 → 11435 변경. 컨테이너 내부 통신(`intelligence-ollama:11434`)은 영향 없음
- 2차 시도: ✓ `intelligence-ollama`, `intelligence-ollama-pull` Started
- 모델 pull(`gemma4:e2b-it-q4_K_M`, `nomic-embed-text`)은 백그라운드 진행

### 15:01 — 02-detection 기동 (env 보간 누락 → 패치)
- 1차 시도: build 성공 후 `network siem-trinity_default not found` — `${LOKI_NETWORK_NAME}` 가 02-detection 디렉토리 `.env` 부재로 기본값 fallback
- 원인: `env_file: ../.env` 는 *컨테이너 내부* 환경변수 지정이며, *compose 파일 변수 보간*은 같은 디렉토리의 `.env` 만 읽음 (서로 다른 메커니즘)
- **패치**: `ln -sf ../.env 02-detection/.env` (심볼릭 링크 — 코드 변경 없음)
- 2차 시도: ✓ `detection-api` Started, 포트 `100.x.x.x:2027->8000`

### 15:04 — 검증 결과

| 항목 | 상태 |
|---|---|
| 컨테이너 (`loki`, `promtail`, `wazuh-manager`, `prometheus`, `node-exporter`, `grafana`, `intelligence-ollama`, `detection-api`) | 모두 Up ✓ |
| 볼륨 `security-log-monitor_*` 4개 | 기존 데이터 재사용 ✓ |
| Loki `/ready` | 200 ✓ |
| Loki 라벨 (기존 `agent_name`, `banned_ip`, `jail`, `rule_id`, `src_ip` 등) | 정상 응답 ✓ — **청크 보존 확인** |
| Grafana `/api/health` | 200 ✓ |
| Grafana 대시보드 (`kangminlog MASTER`, `보안 대시보드`, `syslog`, `Zeek NIDS`) | 4개 모두 보존 ✓ |
| Prometheus `/-/healthy` | 200 ✓ |
| detection-api `/docs` (Swagger) | 200 ✓ (`/health` endpoint 는 부재 — 부수적, 차후 추가 권장) |

### 15:29 — 추가 패치 (HOST_BIND_IP 미적용 발견)
사용자 검증 단계에서 발견: 01-collection·03-intelligence 컨테이너가 `100.x.x.x` (Tailscale IP) 가 아닌 `127.0.0.1` 에만 노출됨 → Tailscale 망 다른 기기에서 Grafana 접근 불가.

원인: 02-detection 과 동일 — `${HOST_BIND_IP}` 변수 보간이 서브디렉토리 `.env` 를 봄. 루트 `.env` 가 자동 전파 안 됨.

조치:
```
ln -sf ../.env 01-collection/.env
ln -sf ../.env 03-intelligence/.env
cd 01-collection && docker compose up -d   # 컨테이너 recreate, 볼륨 보존
cd 03-intelligence && docker compose up -d
```

검증 결과:
```
3000/tcp -> 100.x.x.x:3000   # Grafana
3100/tcp -> 100.x.x.x:3100   # Loki
9090/tcp -> 100.x.x.x:9090   # Prometheus
11434/tcp -> 100.x.x.x:11435 # intelligence-ollama
8000/tcp -> 100.x.x.x:2027   # detection-api
```

→ Tailscale 접근성 200 OK (Loki `/ready` 는 재기동 직후라 잠시 503, 곧 회복)

### 15:36 — 04-ui (TrinitySOC) 가동 — 통합 운영자 진입점 완성

03-intelligence 까지 백엔드 통합이 끝났으나 사용자의 "하나의 URL 통합" 요건을 충족하려면 [04-ui/CLAUDE.md §5](../04-ui/CLAUDE.md) 의 설계대로 nginx BFF 게이트웨이 가동이 필요. 진행:

```bash
cd ~/SIEM-Trinity/04-ui
npm install                                   # 의존성 설치
npm run build                                  # vite production build (4.69s)
cd deploy
ln -sf ../../.env .env                         # 다른 서브디렉토리와 동일 패턴
docker compose up -d                           # nginx:1.27-alpine
```

검증:

| 경로 | 응답 | 의미 |
|---|---|---|
| `http://100.x.x.x:5173/` | 200 | React SPA 정상 |
| `/index.html` | 200 | 정적 자산 정상 |
| `/api/system/host` | 200 | BFF 프록시 → detection-api 정상 |
| `/api/alerts` | 200 | BFF 프록시 정상 |
| `/api/llm/`, `/api/topk`, `/api/health` | 404 | endpoint 부재 (각 prefix 아래 path 만 동작) |

**Trinity SOC 통합 콘솔 단일 진입점**: `http://100.x.x.x:5173/`

## 결과

✅ **cut-over 완전 완료.** 운영 데이터 무손실 유지. Tailscale 망 접근성 복원. 통합 운영자 콘솔 가동.

### 최종 노출 포트

```
:5173   TrinitySOC (통합 콘솔)        ← 운영자 단일 진입점
:2027   detection-api (직접 API 접근용)
:3000   Grafana (시각화, 별개 유지)
:3100   Loki   (BFF 미통과 직접 쿼리용)
:9090   Prometheus
:11435  intelligence-ollama
```

## 후속 권장 (별도 이슈로 분리)

1. **[03-intelligence/docker-compose.yml](../03-intelligence/docker-compose.yml) 에 `env_file: ../.env` 추가** — 02-detection 과 대칭화. 호스트 포트도 `.env` 의 `HOST_BIND_IP` 영향을 받게 정렬
2. **02-detection 의 `.env` 심볼릭 링크 정착** — [start.sh:180](../start.sh#L180) 가 `cd 02-detection && docker compose up -d` 만 호출하는데, compose 파일 변수 보간이 루트 `.env` 를 못 봄. 두 가지 안: (a) `ln -sf ../.env 02-detection/.env` 를 setup 가이드에 명시, (b) start.sh 가 `--env-file ../.env` 명시
3. **detection-api `/health` endpoint 추가** — 헬스체크 모니터링 정렬용
4. **03-intelligence ollama ↔ dodgers-ollama 통합 검토** — 같은 호스트에 ollama 2개는 자원 중복. 모델 공유 가능하면 단일 인스턴스로 합치는 게 효율적
5. **기존 디렉토리 보존 기간** — `~/security-log-monitor`, `~/siem-ai-detector`, `~/siem-ai-analyst` 는 최소 1주일(2026-05-29) 까지 보존. restic 백업 + 신규 운영 안정성 검증 후 archive 디렉토리로 이동

## 롤백 가이드 (사용 안 함, 보존)

문제 발생 시:
```bash
cd ~/SIEM-Trinity/02-detection && docker compose down
cd ~/SIEM-Trinity/03-intelligence && docker compose down
cd ~/SIEM-Trinity/01-collection && docker compose down

cd ~/security-log-monitor && docker compose up -d
cd ~/siem-ai-detector   && docker compose up -d
```
볼륨 동일 → 데이터 그대로 복귀.

